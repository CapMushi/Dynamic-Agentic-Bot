# Dynamic Agentic Systems - Environment Configuration
# Copy this file to .env.local and fill in your values

# API Configuration
# TODO: Set these when backend is ready in Phase 4
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# Development Mode
# Set to 'mock' to use mock APIs, 'development' for local backend, 'production' for live backend
NEXT_PUBLIC_MODE=mock

# Backend Authentication (Phase 4)
# TODO: Implement authentication when backend is ready
NEXT_PUBLIC_AUTH_ENABLED=false
NEXT_PUBLIC_AUTH_URL=

# LLM Provider API Keys (Phase 3)
# TODO: Implement secure API key management
OPENAI_API_KEY=
CLAUDE_API_KEY=
DEEPSEEK_API_KEY=

# Vector Database Configuration (Phase 4)
# TODO: Configure when Pinecone integration is ready
PINECONE_API_KEY=
PINECONE_ENVIRONMENT=
PINECONE_INDEX_NAME=

# File Upload Configuration
# TODO: Configure when backend file processing is ready
NEXT_PUBLIC_MAX_FILE_SIZE=10485760  # 10MB
NEXT_PUBLIC_ALLOWED_FILE_TYPES=pdf,csv

# WebSocket Configuration (Phase 3)
# TODO: Configure when real-time features are implemented
NEXT_PUBLIC_WS_RECONNECT_INTERVAL=5000
NEXT_PUBLIC_WS_MAX_RETRIES=3

# Analytics and Monitoring (Phase 5)
# TODO: Configure when monitoring is implemented
NEXT_PUBLIC_ANALYTICS_ENABLED=false
NEXT_PUBLIC_ANALYTICS_ID= 